# bfp-ecert
E-Certificate System for Bureau of Fire Protection

# TECHSTACK
XAMPP
MariaDB

Bootstrap
Django
HTML
CSS

# DEPENDENCIES
pip install django
pip install mysqlclient
pip install mysqlite (?)
